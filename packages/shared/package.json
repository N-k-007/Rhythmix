{
  "name": "@rhythmix/shared",
  "version": "1.0.0",
  "description": "Shared utilities, types, and components for Rhythmix",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zustand": "^4.4.0",
    "immer": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
